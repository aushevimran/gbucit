import React from "react";

import Feedback from "../Feedback";

const Account = ({ itemuser }) => {
  //   console.log({ itemuser });
  return (
    <div className="account">
      <div className="wrapper__container">Account</div>
    </div>
  );
};

export default Account;
